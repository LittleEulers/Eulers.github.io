# Eulers.github.io

Simple static website containing a basic calculator.

## Run locally

Open `index.html` in your browser, or serve the project directory:

```bash
python3 -m http.server 8000
```

Then visit <http://localhost:8000>.
